# Python Auto IP Changer

This script automatically rotates IP addresses using the Tor Network.

## Setup Instructions
1. Install Tor Service on your system.
2. Enable ControlPort 9051 in your `torrc` file.
3. Install required Python packages:
   ```bash
   pip install requests stem requests[socks]
   
